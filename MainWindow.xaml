<Window x:Class="WPF_CRUD.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_CRUD"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Database Management"></Label>

        <DataGrid Grid.Row="1" AutoGenerateColumns="False" Name="CzytelnikDG" IsReadOnly="True" Margin="0,0,0,10" Grid.RowSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="Imie" Binding="{Binding imie}"/>
                <DataGridTextColumn Header="Nazwisko" Binding="{Binding nazwisko}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button  Grid.Row="3" Click="openDodajWindow">Dodaj</Button>
    </Grid>
</Window>
